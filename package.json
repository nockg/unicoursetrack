{
  "name": "unitrack",
  "version": "1.0.0",
  "private": true,
  "description": "UniTrack course progress tracker",
  "scripts": {
    "sync": "node scripts/build-assets.mjs",
    "dev": "npm run sync && node scripts/dev-server.mjs",
    "build": "npm run check && npm run sync && node scripts/build-dist.mjs",
    "preview": "node scripts/dev-server.mjs",
    "check": "node scripts/security-check.mjs",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
